export const GameConfig = {
  // ─── Размер поля ──────────────────────────────────────────────────────────
  rows: 9,          // количество строк
  cols: 9,          // количество столбцов

  // ─── Тайлы ────────────────────────────────────────────────────────────────
  colors: 5,        // сколько разных цветов обычных тайлов
  minGroupSize: 2,  // минимальный размер группы для сжигания (меньше — нельзя лопнуть)

  // ─── Супер-тайлы ──────────────────────────────────────────────────────────
  superTileThreshold: 3, // группа >= этого числа → появляется супер-тайл (999 = выключено)
  superTileSpawnChance: 0.05, // вероятность что новый тайл будет супер (0.05 = 5%, 0 = выключено)

  // ─── Бустер Бомба ─────────────────────────────────────────────────────────
  bombRadius: 2,    // радиус взрыва бомбы в клетках (2 = квадрат 5×5)

  // ─── Перемешивание ────────────────────────────────────────────────────────
  maxShuffles: 3,   // сколько раз поле перемешается если нет ходов (потом проигрыш)

  // ─── Цель игры ────────────────────────────────────────────────────────────
  goal: {
    score: 8000,    // сколько очков нужно набрать для победы
    moves: 18,      // сколько ходов даётся игроку
  },

  // ─── Формула очков ────────────────────────────────────────────────────────
  // итого = размер_группы² × multiplier
  // примеры: 2 тайла = 40 очков, 5 тайлов = 250, 9 тайлов = 810
  scoring: {
    multiplier: 8,
  },
};